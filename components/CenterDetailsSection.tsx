type CenterDetailsSectionProps = {
  centerDetails: any;
};

const CenterDetailsSection = ({ centerDetails }: CenterDetailsSectionProps) => {
  console.log(centerDetails);
  return <div>CenterDetailsSection</div>;
};

export default CenterDetailsSection;
